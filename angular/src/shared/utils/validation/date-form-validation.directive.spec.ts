import { DateFormValidationDirective } from './date-form-validation.directive';

describe('DateFormValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFormValidationDirective();
    expect(directive).toBeTruthy();
  });
});
