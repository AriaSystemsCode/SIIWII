import { TextSubstringPipe } from './text-substring.pipe';

describe('TextSubstringPipe', () => {
  it('create an instance', () => {
    const pipe = new TextSubstringPipe();
    expect(pipe).toBeTruthy();
  });
});
